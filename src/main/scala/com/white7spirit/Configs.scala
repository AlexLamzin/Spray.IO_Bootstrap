package com.white7spirit

import scala.concurrent.duration._

object Configs {
  val actorSystemName = "your-api"
  val serviceName = "your-service"

  val defaultTimeout = 5.seconds

  val interface = "localhost"
  val port = 8080
}
